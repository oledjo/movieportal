# Мои Фильмы - Портал для просмотра списка фильмов

Веб-портал для просмотра списка фильмов из Todoist с постерами из TMDB.

## Возможности

- Динамическая загрузка списка фильмов из Todoist проекта "🍿 Фильмы"
- Постеры фильмов из TMDB (The Movie Database)
- Рейтинги Кинопоиска и IMDb
- Фильтрация по категориям (Фильмы, Сериалы, Смотрю сейчас)
- Поиск по названию, режиссёру
- Сортировка по рейтингу, названию, году
- Отображение описания "почему посмотреть" из Todoist
- Адаптивный дизайн

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

Откройте http://localhost:3000 в браузере.

## Настройка API ключей

При первом запуске откроется окно настроек, где нужно ввести:

### Todoist API Token (обязательно)

1. Откройте [настройки Todoist](https://todoist.com/prefs/integrations)
2. Перейдите в раздел "Developer"
3. Скопируйте API token

### TMDB API Key (для постеров)

1. Зарегистрируйтесь на [TMDB](https://www.themoviedb.org/)
2. Перейдите в [настройки API](https://www.themoviedb.org/settings/api)
3. Создайте API ключ (бесплатно)

## Структура проекта Todoist

Портал ожидает проект "🍿 Фильмы" со следующими секциями:
- Правила (игнорируется)
- Смотрю сейчас
- Фильмы
- Сериалы
- 25 главных фильмов XXI века

Формат задач:
- **Название**: "Название фильма (Год) — Режиссёр"
- **Описание**: "Кинопоиск:8.1, 2 ч 9 минут\n\nПочему посмотреть..."

## Сборка для продакшена

```bash
npm run build
```

Файлы будут в папке `dist/`.

## Деплой на GitHub Pages

### Автоматический деплой (рекомендуется)

1. Убедитесь, что репозиторий называется `movie-portal` (или измените `base` в `vite.config.js`)
2. В настройках репозитория GitHub:
   - Settings → Pages → Source: выберите "GitHub Actions"
3. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Setup GitHub Pages deployment"
   git push
   ```
4. GitHub Actions автоматически задеплоит приложение при каждом push в `main`/`master`

### Ручной деплой

```bash
# Установите gh-pages (если еще не установлен)
npm install --save-dev gh-pages

# Задеплойте
npm run deploy
```

**Важно:** Если ваш репозиторий называется не `movie-portal`, измените `base` в `vite.config.js`:
- Для репозитория `my-repo` → `base: '/my-repo/'`
- Для деплоя в корень → `base: '/'`

## Технологии

- Vue 3 + Vite
- Todoist REST API
- TMDB API
